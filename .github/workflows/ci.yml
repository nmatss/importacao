name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  lint-api:
    name: Lint & Typecheck API
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Typecheck API
        run: npx -w apps/api tsc --noEmit
      - name: Lint API
        run: npx -w apps/api eslint src --max-warnings=0

  lint-web:
    name: Lint & Typecheck Web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Typecheck Web
        run: npx -w apps/web tsc --noEmit
      - name: Lint Web
        run: npx -w apps/web eslint src --max-warnings=0

  lint-python:
    name: Lint Python (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install ruff
        run: pip install ruff
      - name: Run ruff
        run: ruff check apps/cert-api/

  test-api:
    name: Test API
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: importacao_test
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Run API tests
        run: npm test -w apps/api -- --coverage
        env:
          DATABASE_URL: postgres://test:test@localhost:5432/importacao_test
      # e2e usa Testcontainers (sobe seu próprio Postgres efêmero via Docker,
      # disponível por padrão no runner ubuntu-latest) e injeta DATABASE_URL/
      # JWT_SECRET a partir do container em test/e2e/setup.ts — por isso não
      # depende do serviço postgres acima nem de env adicional.
      - name: Run API e2e tests
        run: npm run test:e2e -w apps/api
      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: api-coverage
          path: apps/api/coverage/
          retention-days: 7

  test-web:
    name: Test Web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Run Web tests
        run: npm test -w apps/web --if-present

  test-python:
    name: Test Python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install pytest pytest-cov pytest-mock pytest-asyncio httpx -r apps/cert-api/requirements.txt
      - name: Run pytest
        run: pytest apps/cert-api/ --tb=short -q

  security-scan:
    name: Security Audit Node
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: npm audit (blocking on high/critical)
        run: npm audit --audit-level=high

  security-python:
    name: Security Audit Python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install pip-audit
        run: pip install pip-audit
      - name: pip-audit cert-api
        run: pip-audit -r apps/cert-api/requirements.txt

  build-docker:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs:
      [lint-api, lint-web, lint-python, test-api, test-web, test-python, security-scan, security-python]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build API image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./apps/api/Dockerfile
          push: false
          tags: importacao-api:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=docker
      - name: Build Web image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./apps/web/Dockerfile
          push: false
          tags: importacao-web:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=docker
      - name: Build Cert API image
        uses: docker/build-push-action@v6
        with:
          context: ./apps/cert-api
          push: false
          tags: importacao-cert-api:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=docker
      - name: Trivy scan - API image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: importacao-api:ci
          format: table
          exit-code: "1"
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          trivyignores: .trivyignore
      - name: Trivy scan - Web image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: importacao-web:ci
          format: table
          exit-code: "1"
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          trivyignores: .trivyignore
      - name: Trivy scan - Cert API image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: importacao-cert-api:ci
          format: table
          exit-code: "1"
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          trivyignores: .trivyignore
      - name: SBOM - API (syft)
        uses: anchore/sbom-action@v0
        with:
          image: importacao-api:ci
          artifact-name: sbom-api.spdx.json
          output-file: sbom-api.spdx.json
      - name: SBOM - Web (syft)
        uses: anchore/sbom-action@v0
        with:
          image: importacao-web:ci
          artifact-name: sbom-web.spdx.json
          output-file: sbom-web.spdx.json
      - name: SBOM - Cert API (syft)
        uses: anchore/sbom-action@v0
        with:
          image: importacao-cert-api:ci
          artifact-name: sbom-cert-api.spdx.json
          output-file: sbom-cert-api.spdx.json

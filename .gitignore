node_modules/
dist/
.env
*.log
uploads/
.DS_Store
coverage/
*.tsbuildinfo
certifications.db

# SOPS encrypted file — commit only when encrypted
# Unencrypted version must never be committed
.env.sops.yaml

# Deploy artifacts
deploy.log
scripts/deploy.sh.backup
.claude/worktrees/

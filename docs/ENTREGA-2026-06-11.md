# Registro de Entrega — 2026-06-11

**PR:** [#58](https://github.com/nmatss/importacao/pull/58) (merged) · **Em produção:** SHA `96a695c` em 192.168.168.124 · **Pendências:** issues [#60](https://github.com/nmatss/importacao/issues/60)–[#63](https://github.com/nmatss/importacao/issues/63)

Este documento consolida tudo que entrou em produção em 2026-06-11 — da revisão 100% do projeto até o deploy verificado. Complementa: [`ODETT-STATUS.md`](./ODETT-STATUS.md) (status por ponto da UAT), [`REVISAO-100.md`](./REVISAO-100.md) (backlog vivo), [`AI-HARNESS.md`](./AI-HARNESS.md), [`CERT-LINX-WRITE.md`](./CERT-LINX-WRITE.md) e `CHANGELOG.md`.

---

## 1. O que entrou (4 blocos, 7 commits)

### 1.1 UAT da Odett + camada de confiança (commits anteriores do PR, verificados nesta rodada)

Os 10 pontos da UAT (processo IM0712602NB) foram re-verificados ponto a ponto contra o código com evidência de arquivo/linha — todos confirmados. Detalhe em `ODETT-STATUS.md` §1.

### 1.2 Cadastro de certificado + escrita gated no Linx (`549dec8`)

- Tela `/certificacoes/cadastro` (marca, SKU, datas, nº cert, OCP, órgão, PDF) + lista de recentes + "Reenviar ao Linx"
- cert-api: 5 endpoints `/api/certificates*`; tabela `cert_certificates`; upsert anti-race em `PROP_PRODUTOS` (Puket `00224`/`00225`, Imaginarium `00106`/`00107`)
- **Fail-closed**: `LINX_WRITE_ENABLED=false`; resolução SKU→produto recusa gravar sem `LINX_PRODUTO_COL_SKU`; PDF validado por magic bytes; 31 testes
- Go-live: issue #62 — descoberta agora é CLI (`scripts/linx_discovery.py`), sem SSMS

### 1.3 Hardening de segurança/CI (`f03706b`, `bc27049`, `5fa262a`)

- **CI com gates reais**: removidos os `|| true` de lint (api/web) e pytest do cert-api; 42 warnings de ESLint zerados; pytest do CI com deps completas
- **SMTP**: `rejectUnauthorized` deixou de ser `false` hardcoded (política por ambiente + override explícito `SMTP_TLS_REJECT_UNAUTHORIZED`)
- **Login fail-closed**: `GOOGLE_GROUP_ALLOWED` vazio nega acesso (allow-all antigo só com `GOOGLE_GROUP_ALLOW_ALL_WHEN_UNSET=true`)
- **CodeQL**: 4 alertas novos tratados (rate limit nas rotas financeiras, sanitização inline de log no cert-api, 1 falso positivo dispensado com justificativa)

### 1.4 Parte A — domínio (`444946d`)

| Frente                                      | O que faz                                                                                                                                                                                                                                                                                    | Limites conhecidos                                                                                                   |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| **Motor financeiro** (`modules/financial/`) | Valor Aduaneiro = (FOB+frete+seguro)×USD; Numerário = ×0,6; `%numerário`; `GET/POST /api/processes/:id/financials[/recompute]`; job diário 08:30 com alertas **Invoice baixa** (<US$ 20k), **Seguro** (>US$ 150k → "ACIONAR SEGURADORA") e **demurrage** (free time ≤7d/vencido), com dedupe | Sem tarifa diária de demurrage por terminal (Premissas não tem); conversão BRL exige `currency_exchanges` alimentada |
| **Historização** (migration `0015`)         | `validation_result_history` (snapshot append-only antes do delete+recreate) e `document_extraction_history` (aiParsedData arquivado antes de reprocess/re-extração); `GET .../validation-history` e `GET .../extraction-history`                                                             | Política de retenção em aberto                                                                                       |
| **Espelho por EAN** (groundwork)            | `ean` por item na INV/PL (anti-alucinação), validador GTIN no harness, join PL×INV por EAN com fallback itemCode, enriquecimento pela Base EAN Puket (só candidato único; `eanSource: document\|kb`)                                                                                         | Layout do XLSX **inalterado** (alinhar com a Odett); Base EAN Imaginarium quebrada na origem (`#REF!`)               |

### 1.5 Operação/deploy (`444946d` + docs)

- `deploy.sh` aplica migrations automaticamente (passo 4.5, `apply-pending-migrations.sh`, 0011→0015)
- Pasta do Drive para a Pre-Cons criada: **"Pre-Cons (sync portal importação)"** (`1OJmEV1GTI7vC0B-Uxb-btgQRMDu0530B`) — issue #61

## 2. Verificação (gates no momento do merge)

| Gate                                          | Resultado                                         |
| --------------------------------------------- | ------------------------------------------------- |
| `tsc --noEmit` api + web                      | limpo                                             |
| Testes                                        | **344 api · 26 web · 86 cert-api** — todos verdes |
| ESLint (`--max-warnings=0`, agora bloqueante) | 0                                                 |
| ruff (cert-api)                               | limpo                                             |
| `npm audit --audit-level=high`                | 0 high/critical                                   |
| CI completo (inclui CodeQL e Trivy)           | verde                                             |

## 3. Deploy (2026-06-11, ~12:49 BRT)

1. Pré-condições no servidor verificadas (`GOOGLE_GROUP_ALLOWED` configurado → fail-closed seguro)
2. **Teste real do Vertex antes de ligar**: `countTokens` do servidor → **403** (SA sem `roles/aiplatform.user`) → mantido OpenRouter; Vertex pré-configurado no `.env` (issue #60)
3. Backup do banco (obrigatório no script) → rsync → build → **migrations automáticas** → health verde
4. Pós-deploy verificado: tabelas de historização criadas, `resolution_note` presente, scheduler com financial-check 08:30 e pre-cons-sync 6h, containers healthy, zero erros no log
5. `.env` do servidor (backup `.env.bak-20260611`): + `GOOGLE_DRIVE_PRE_CONS_FOLDER_ID`, `AI_MONTHLY_BUDGET_USD=26`, `GOOGLE_VERTEX_PROJECT/LOCATION`, `AI_PROVIDER=vertex` comentado

## 4. Pendências (rastreadas em issues)

| Issue                                                 | O quê                                                                                          | Dono               |
| ----------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------ |
| [#60](https://github.com/nmatss/importacao/issues/60) | Ligar Vertex: habilitar API + `roles/aiplatform.user` no GCP, descomentar `AI_PROVIDER=vertex` | Nicolas (GCP)      |
| [#61](https://github.com/nmatss/importacao/issues/61) | Compartilhar a pasta Pre-Cons com a SA (leitor) + time salvar as semanais lá                   | Nicolas + operação |
| [#62](https://github.com/nmatss/importacao/issues/62) | Descoberta Linx (`linx_discovery.py`) + preencher `LINX_*` + ligar escrita                     | Nicolas (DBA)      |
| [#63](https://github.com/nmatss/importacao/issues/63) | `NODE_ENV=development` em prod, TLS no compose, SOPS/age key, rotação de chave da SA           | Nicolas (infra)    |

Itens maiores fora de issue (decisão de negócio): layout do espelho ancorado em EAN (alinhar com a Odett), tarifas de demurrage por terminal, Base EAN Imaginarium, fluxos Back-to-Back/Exportação, relatório de licenciamento com estoque (ver `REVISAO-100.md` §2).

## 5. Operação — referência rápida

```bash
# Deploy (da máquina do Nicolas, master limpo)
bash scripts/deploy.sh                      # backup + rsync + build + migrations + health

# Descoberta Linx (no servidor)
docker exec importacao-cert-api python scripts/linx_discovery.py puket <SKU-real>

# Migrations manuais (fallback)
/home/nicolas/importacao/scripts/apply-pending-migrations.sh
```

Servidor: host `n8n` (192.168.168.124), user `nicolas`, dir `~/importacao` (deploy por rsync, não é repo git). API prod na 3050, web na 8085, cert-api na 8000 (interno).

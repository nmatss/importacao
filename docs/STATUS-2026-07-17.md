# STATUS 2026-07-17 — Confiança honesta da IA, auditoria docs+UX executada em 3 rodadas, fix Eduarda

Doc canônico do dia. Produção começou em `915db83` e terminou em **`b55968a`** (6 deploys
verificados, api/web/cert healthy). Auditoria completa em
[REVISAO-DOCS-UX-2026-07-17.md](REVISAO-DOCS-UX-2026-07-17.md).

---

## 1. Respostas operacionais (Eli, Suelen/Lilian)

**Eli — "os campos do site ficam em alguma tabela?"** Parcialmente: SKU/nome/marca/prazo
vivem em `cert_products` (Postgres), mas os 4 status (certificação, ecommerce,
licenciamento) são **derivados em runtime** — não existem em tabela. O confronto com a
propriedade do Broilo é contra `PROP_PRODUTOS` no Linx: Puket `00224`/`00225`,
Imaginarium `00106`/`00107` (validade do certificado / vencimento do licenciamento),
valor texto `dd/mm/aaaa` em `VALOR_PROPRIEDADE`, PK tripla com `ITEM_PROPRIEDADE=1`.
Ferramenta de confronto pronta: `docker exec importacao-cert-api python
scripts/sync_prazo_venda_linx.py --list`. Ver [CERT-LINX-WRITE.md](CERT-LINX-WRITE.md).

**Suelen/Lilian — acesso aos relatórios.** Liberar acesso = adicionar o e-mail ao Google
Group `importacao@grupounico.com` (auto-provisiona como `analyst` no 1º login Google;
`analyst` já vê Relatórios inteiro). **NÃO usar o botão "Novo Usuário"** — cria a linha
mas a pessoa continua sem conseguir logar (o gate do grupo roda antes). Pendência de
produto: **relatório de licenciamento NÃO existe** — os 4 exports são todos de
certificação/estoque; o dado de licenciamento só aparece na tela de Produtos (derivado da
aba "Licenciamentos Vencidos" a cada request). `GET /api/licenciados` existe sem consumidor.

## 2. IA/Vertex — análise profunda + execução (commits `2339490`, `735bc3d`)

Estado verificado em prod: `AI_PROVIDER=vertex` estável desde 22/06, uso ínfimo
(~US$0,15/3 semanas), gargalo = qualidade do input, não custo. **Issue #60 fechada**
(estava aberta por engano); #100 criada (corpus real de acurácia + few-shot real +
generalizar parser KIOM — dependem de curadoria humana).

Implementado e **deployado**:

- **Confiança honesta**: grounding pulado (scan sem OCR) marca `_trust.groundingSkipped`
  e capa em 0.75; cobertura ponderada (campo esperado null pesa ¼ — extração quase vazia
  não pontua mais 95%); Σitens≠FOB e líquido>bruto viram `error` → revisão (0.39).
- Revisão adversarial do próprio diff pegou 3 bugs: cap de review era **lavado pela
  reconciliação** (agora `_trust.trust==='review'` capa no `computeConfidenceScore`);
  merge de `_trust` invertido (report fresco vence — self-repair limpa review corrigido);
  pseudo-campos `_grounding`/`_contract` filtrados do alerta.
- **Telemetria consolidada**: `governance.ts` (in-memory, leitores mortos) deletado →
  `ai_usage_log` ganhou `latency_ms`/`prompt_version`/`error_message` (migração 0025);
  erros SEMPRE persistidos; `/api/ai/usage` expõe latência/erros por modelo;
  `/health/live` expõe `aiProvider`+`revision` (mata o drift repo-vs-prod).
- `AI_SELF_REPAIR_PAID=1` ligado (SOPS + compose — lembrete: compose lista env
  explicitamente). Corpo de erro HTTP truncado no log dos 3 providers.
- **Cache por hash descartado com justificativa**: lease (0024) + dedup sha256 já cobrem
  redundância; reprocess explícito deve re-rodar.

Trade-offs aceitos: divergência aritmética benigna quarentena o doc até revisão (observar
volume de alertas); docs esparsos disparam 1 tentativa Pro extra (centavos, gated).

## 3. Auditoria docs+UX → executada em 3 rodadas com revisão cada

### R1 — dados confiáveis (`763e71d`, + infra do Nicolas `d4de75e`)

- **Falsos verdes mortos**: espelho auto-gerado (cópia da Invoice/PL) fora da conferência
  do comparativo e do check OHBL×Espelho, com banner explicativo e `espelhoSource` no
  payload; linha de 1 fonte vira status neutro `single_source` ("Fonte única"); conflitos
  da reconciliação viram alerta (com `hasActiveAlert` + doc.id no título); re-projeção da
  reconciliação respeita o piso 0.4 (`documents/constants.ts` é a fonte única);
  `getExtractionEvidence` volta a servir o run de extração (com página/trecho).
- **Pipeline de leitura**: fila `ai-extraction` com `retryLimit=2`+`expireInSeconds=25min`
  (crash duro não prende mais documento); lease 10→25min; timeout próprio de 20min para
  `extractText`; detecção de escaneado por densidade alnum/página via `data.numpages`
  (o split por `\f` NUNCA contou páginas — pdf-parse junta com `\n\n`); teto 15MB para
  multimodal; classificação palavras-fortes → BL → tokens de referência (mata o misclass
  OHBL-como-invoice; front alinhado via `DETECTION_RULES`); e-mail 100% ignorado por
  formato e anexos órfãos geram alerta (assunto no título p/ não colidir no dedup).
- Revisão pegou 8 defeitos antes do commit (aceite 400 do `single_source`, densidade
  inócua, timeout matando OCR legítimo, alerta falso p/ duplicata, dedup global,
  tempestade de backfill, divergência front/back, `[object Object]` na rota HTTP).

### R2 — bugs de fluxo UX (`b501405`)

Toggle usuários (`isActive`), SSE da validação cert destrava no erro (stream fechado sem
evento final também), deep-links do dashboard cert filtram
(`?cert_status=ENCERRADO`/`?site_status=NAO_CONFORME`), toasts em
EmailIngestion/Agendamentos/Alertas, paginação real de Alertas (campo é
`pagination.pages`), ConfirmDialog no envio de e-mail + multi-destinatário, sessão
expirada com aviso e returnTo (logout limpa), retry do Linx com feedback. Revisão pegou 6.

### R3 — fundação (`518a6fa`)

- **AppLayout único**: os 2 layouts (~73% idênticos) viraram wrappers finos; collapse
  persistido em localStorage, switcher direto entre módulos, breadcrumb de detalhe nas
  telas cert.
- **`shared/lib/confidence.ts`**: 4 cópias do piso 0.4 + DocBadge 0.6 unificados;
  ConfidenceBadge explica **o porquê** da confiança baixa (tooltip via `_trust`) com ação
  sugerida.
- Primitivos `Button`/`Card` criados e em uso (paginação de Alertas); migração das telas
  existentes é incremental. Revisão pegou 4 (borda de arredondamento, 4ª cópia do 0.4,
  trap de null, trailing slash).

## 4. Fix Eduarda — "Registro concedido" e histórico multilinha (`b55968a`)

PI7550Y/51Y/53Y (+23 = **26 produtos**) exibiam Encerrado/Não Conforme com certificação
ativa: o `sheet_status` "01/09/25 - Registro concedido no Orquestra." não era
reconhecido, e o fallback conservador condenava. Além disso o campo frequentemente traz o
**histórico inteiro** da planilha (multilinha, mais recente primeiro) e o substring no
texto todo deixava entrada velha ("Registro encerrado" 2024) vencer a recente
("Manutenção Finalizada" 2026). Fix em `derive_cert_status`: **a primeira linha decide**
quando tem marcador; "conce" (concedido/concedida/typo "concecida") é marcador ativo com
guarda de expiração; exclusão segue terminal em qualquer linha. Verificado em prod: os 3
SKUs → ATIVO/CONFORME; PI7555Y ("Item excluído") segue ENCERRADO corretamente. cert-api
400 testes.

## 5. Operação/deploy — aprendizados do dia

- `scripts/deploy.sh` tem **prompt interativo** — em automação usar `echo y | ...`
  (o 1º deploy "sucedeu" com exit 0 sem fazer nada).
- **O deploy NÃO roda migrações** — a 0025 foi aplicada manualmente
  (`docker cp` + `psql -f`; ou `/opt/importacao/scripts/apply-pending-migrations.sh`).
- Novo health check do proxy no deploy (browser→nginx→api) — fix do Nicolas pós-incidente
  16/07, protege contra "deploy ok com login fora do ar".
- Badges de confiança ficarão **menores e mais honestos** — avisar Odett/Eduarda; o
  tooltip agora explica a causa.

## 6. Pendências (donos)

| Pendência                                                                                                                             | Dono                    |
| ------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| SA dedicada `gemini-n8n` no GCP + remover `OPENROUTER_BASE_URL` da Developer API                                                      | Nicolas (GCP/SOPS)      |
| Corpus real anonimizado p/ acurácia + few-shot (issue #100)                                                                           | Nicolas + Odett/Eduarda |
| Relatório de licenciamento (export) — confirmar necessidade com Lilian/Suelen                                                         | Nicolas                 |
| Reteste Eduarda: cadastro + prazos Linx; badge fantasma "Vendido - Venda Bloqueada" = provável cache do navegador (Ctrl+F5)           | Eduarda                 |
| Conta de serviço p/ `ERP_*_USER` do Linx (hoje conta pessoal)                                                                         | Nicolas/TI              |
| Backlog técnico remanescente (OCR default, filtros na URL, migração Button/Card, LI extractor, tolerâncias de data, linhagem em tela) | rodadas futuras         |

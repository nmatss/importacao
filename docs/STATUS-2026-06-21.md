# STATUS — 2026-06-21 (feedback Eduarda: finalização + verificação adversarial)

> Documento canônico da rodada. Sucede `docs/STATUS-2026-06-20.md`.

## TL;DR

Feedback da Eduarda (qui. 17:35/18:13) auditado item a item por **time multi-agente
(18 bullets → audit + verificação adversarial, 36 agentes)** contra o código **já em
produção**. Conclusão central:

- **A maioria do feedback já estava implementada em `master` e só faltava o deploy**
  — que **já havia ocorrido** quando a sessão começou (prod `REVISION = fa75ffe`,
  todos os containers `healthy`). A Eduarda testou contra uma build de produção
  **atrasada**; por isso via falhas já corrigidas no código.
- Os verificadores adversariais **derrubaram 4 "done" para defeitos reais de código**
  → corrigidos em **PR #99** (`fix/eduarda-residual-gaps-2026-06-21`, merge `df13229`).
- Itens restantes são **externos** (config/IAM) ou **enhancement** (redesenho de backend).

## Estado de deploy

| Ref                        | SHA           | Observação                                                                                                                                                                                                               |
| -------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Prod no início da sessão   | `fa75ffe`     | PR #98 já deployado (deploy completou antes do WSL fechar; `deploy.sh` só grava `REVISION` após health checks)                                                                                                           |
| `master` após PR #99       | `df13229`     | inclui as 4 correções residuais                                                                                                                                                                                          |
| **Prod final (deployado)** | **`df13229`** | `echo y \| ALLOW_SYDLE_SYNC_DEPLOY=1 bash scripts/deploy.sh`. Health checks api/cert-api/web verdes, todos os containers `healthy`, backup pré-deploy ok. `web` não foi recriado (mudanças só de api → imagem idêntica). |

Suítes verdes: **api 726 (+7) · web 103 · cert-api 335** · typecheck · lint.

## Veredito por item (18 bullets)

### Já feito em `master` (Eduarda precisa **retestar** na build atual)

| #   | Item                                                      | Evidência                                                                                                                             |
| --- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 6   | Popup (hover) com comentários dos Aceites                 | `ValidationChecklist.tsx:743`, `DocumentComparison.tsx:577` (tooltip `group-hover`)                                                   |
| 7   | Fundir "Cruzamento" no "Comparativo Geral"                | `DocumentComparison.tsx:892-987`; `FupComparisonPanel` removido                                                                       |
| 8   | Peso líquido/bruto por item                               | schemas `invoice/packing-list-response`; `DocumentComparison.tsx:1138`                                                                |
| 9   | Erro "Destinatário não autorizado"                        | `communications/service.ts:130` `isRecipientAllowed()` — código OK; **envio real ainda exige cadastrar destinatários** (ver Externos) |
| 11  | Nome da pessoa no log do "feito"                          | `validation/service.ts:664` join `users.name` → `resolvedByName`                                                                      |
| 13  | Espelho "Cannot read properties of undefined (map)"       | guard `espelho?.items ?? []` (`EspelhoPreview.tsx:262`) + backend sempre array                                                        |
| 14  | Filtros de certificação alinhados aos status novos        | `CertProdutosPage.tsx:54-118` (3 eixos) + filtro server-side                                                                          |
| 15  | Status Certificação só Ativo/Encerrado (PI4257Y, PI5101Y) | `derivation.py:84-138` — "Em andamento" → ENCERRADO                                                                                   |
| 16  | Status Ecommerce só Conforme/Não-conforme + frase         | `derivation.py:160-220` (sem "Pendente"; frase obrigatória em NAO_CONFORME)                                                           |
| 17  | Status Licenciamento via aba "Licenciamentos Vencidos"    | `erp_service.py:401-502` + `derivation.py:222-252` (não replica certificação)                                                         |
| 18  | Licen. Prazo via "Licenciamentos Vencidos"                | idem #17                                                                                                                              |

### Defeitos reais corrigidos (PR #99)

| #      | Defeito                                                                                           | Correção                                                                                                                    |
| ------ | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| **2**  | Draft BL não extraía `containerType` (só o OHBL) → "Container" vazio em processos só com Draft BL | `containerType` no schema + prompt do Draft BL; `build-espelho` usa `(ohbl ?? draft_bl)`                                    |
| **12** | Proforma sem itens com NCM perdia PI/FOB (short-circuit determinístico só dispara com `items>0`)  | `fillProformaNullsFromText` preenche escalares no caminho do LLM (+3 testes)                                                |
| **10** | Comparativo ETD parou de considerar datas de Invoice/PL (removido em `fdd17d9`)                   | fallback `invoiceDate`/`packingListDate` com tolerância ampla (match ≤45d, warn ≤90d); `computeRowStatus` aceita `dateOpts` |
| **1**  | Transição "em trânsito" (ETD no passado) sem teste de regressão                                   | novo `logistic-auto-advance.test.ts`                                                                                        |

### Externos (dependem de config/IAM — fora de código)

| #     | Item                                                       | Ação                                                                                                                                             |
| ----- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 4 / 9 | Qualidade de extração ("78% da Invoice") e envio de e-mail | **Vertex IAM 403 (#60)** para extração; **cadastrar Destinatários operacionais / `COMMUNICATION_ALLOWED_RECIPIENTS` (#78)** para liberar o envio |

### Enhancement / parcial (backlog, exige redesenho)

| #   | Item                                               | Nota                                                                                                                                                  |
| --- | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3   | Coluna "Sistema" nos demais quadros                | Painel "Documentos vs Sistema" já preenchido; coluna por **item** exige validações de sistema em nível de item no backend                             |
| 4   | Campos do Packing List não lidos                   | `fillInvoiceNullsFromText` existe; falta `fillPackingListNullsFromText` simétrico (escalares de header). Teto maior continua no modelo → Vertex (#60) |
| 5   | Reconciliação de anomalias (7-vs-1) sob erro de BD | comportamento **intencional** (fail-open: mantém anomalias da IA durante outage em vez de escondê-las). Sem mudança                                   |

## Próximas ações (todas externas / Nicolas)

1. **Eduarda retesta** a build atual (a maioria do feedback já está no ar).
2. **Cadastrar destinatários operacionais** (#78) para liberar envio de e-mail.
3. **Destravar Vertex IAM 403** (#60) para subir o teto de extração (Invoice escaneada/itens, Packing List).
4. Backlog: `fillPackingListNullsFromText` + coluna "Sistema" por item.
